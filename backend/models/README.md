# Trained model files

Copy the files you downloaded from the Colab notebooks here:

- `model1_resume_classifier_baseline.joblib`  (from Model 1 notebook)
- `model2_skill_baseline.joblib`              (from Model 2 notebook)

These power /api/v1/resume/classify (Model 1) and /api/v1/skill-gap/* (Model 2).
(.joblib files are git-ignored — they are artifacts, not source.)
